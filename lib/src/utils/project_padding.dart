import 'package:flutter/material.dart';

class ProjectPadding {
  final lowPadding = const EdgeInsets.all(10);
  final mediumPadding = const EdgeInsets.all(20);
  final highPadding = const EdgeInsets.all(30);

  final lowButtonPadding = const EdgeInsets.all(3);
  final mediumButtonPadding = const EdgeInsets.all(5);
  final highButtonPadding = const EdgeInsets.all(7);

  final verticalxxsPadding = const EdgeInsets.symmetric(vertical: 3);
  final verticalLowPadding = const EdgeInsets.symmetric(vertical: 20);
  final verticalMediumPadding = const EdgeInsets.symmetric(vertical: 30);
  final verticalHighPadding = const EdgeInsets.symmetric(vertical: 40);

  final symmetricxxsPadding = const EdgeInsets.symmetric(vertical: 2, horizontal: 5);
  final symmetricLowPadding = const EdgeInsets.symmetric(vertical: 3, horizontal: 10);
  final symmetricMediumPadding = const EdgeInsets.symmetric(vertical: 5, horizontal: 20);
  final symmetricHighPadding = const EdgeInsets.symmetric(vertical: 7, horizontal: 30);
}
